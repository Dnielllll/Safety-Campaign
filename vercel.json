{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install && cd ../api && npm install",
  "functions": {
    "api/*.js": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/send-notification",
      "destination": "/api/send-notification"
    },
    {
      "source": "/api/send-otp",
      "destination": "/api/send-otp"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
